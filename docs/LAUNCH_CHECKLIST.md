# 有料公開チェックリスト

発話フィットネスを合同会社LearnK のサービスとして `https://speech-fitness.learn-k.net` で有料公開するまでに必要なものの一覧です。
「済」はリポジトリ内で用意できたもの、「要作業」は管理画面などで人が行う作業です。

## 1. ドメインと配信

| 項目                                                                                                          | 状態   |
| ------------------------------------------------------------------------------------------------------------- | ------ |
| 公開ドメインを `speech-fitness.learn-k.net` に決定（`learn-k.net` は お名前.com の DNS、Vercel 向けに運用中） | 済     |
| コード内の公開 URL（`src/constants/site.ts`）、OGP、canonical、特商法の販売 URL                               | 済     |
| Vercel のプロジェクト設定 → Domains に `speech-fitness.learn-k.net` を追加                                    | 要作業 |
| お名前.com の DNS に CNAME `speech-fitness` → `cname.vercel-dns.com` を追加                                   | 要作業 |
| 本番デプロイ後、`/`, `/welcome`, `/pricing`, `/terms`, `/privacy`, `/legal` が開くことを確認                  | 要作業 |

## 2. 法務ページ

| 項目                                                                                          | 状態   |
| --------------------------------------------------------------------------------------------- | ------ |
| 利用規約 `/terms`（`src/content/terms.ts`）                                                   | 済     |
| プライバシーポリシー `/privacy`（`src/content/privacy.ts`）                                   | 済     |
| 特定商取引法に基づく表記 `/legal`（`src/content/tokushoho.ts`）                               | 済     |
| 全画面フッターから法務ページへのリンク（`AppFooter`）                                         | 済     |
| 会社情報の確認: 登記上の商号（LearnK / ランケイ）、代表者名、電話番号、受付時間               | 要確認 |
| 所在地の扱い: 現在は「千葉県松戸市（請求があれば開示）」。番地まで載せるなら `site.ts` を変更 | 要判断 |
| 管轄裁判所: 東京地方裁判所（SQL リテラシー診断テストと同じ）。変えるなら `terms.ts` 第15条    | 要判断 |
| 内容の最終確認（可能なら専門家のレビュー）                                                    | 要作業 |

法務ページの文面は `src/content/` にデータとして置いてあります。金額と無料期間は `src/lib/plans.ts` から自動で埋まります。文面を変えたら `src/constants/site.ts` の `LEGAL_UPDATED` も更新してください。

## 3. ロゴ・アイコン・OGP

| 項目                                                                         | 状態 |
| ---------------------------------------------------------------------------- | ---- |
| ロゴマーク SVG（`assets/brand/`）とアプリ内ヘッダーへの表示                  | 済   |
| iOS / Android / Expo Go 用アイコン、スプラッシュ画像                         | 済   |
| Web ファビコン（`favicon.ico`, 32px, 192px）、Apple touch icon、PWA manifest | 済   |
| OGP 画像 `public/og-image.png` と `<head>` のメタタグ（`src/app/+html.tsx`） | 済   |
| Expo テンプレートの iOS Icon Composer アイコン（`assets/expo.icon`）を削除   | 済   |

## 4. ランディングページ

| 項目                                                                                           | 状態 |
| ---------------------------------------------------------------------------------------------- | ---- |
| `/` を初回訪問者向けランディング、再訪者とログイン済みはホームに自動で切替                     | 済   |
| `/welcome` で常にランディングを表示（フッターやシェア用）                                      | 済   |
| 構成: ヒーロー → 結果イメージ → 測る・鍛える・見える化 → 使い方 → 8 ドリル → 料金 → 安心 → FAQ | 済   |
| 実際の画面のスクリーンショットに差し替え（現在は結果イメージを CSS で描画）                    | 任意 |
| Google Search Console への登録、`_sitemap` の確認                                              | 任意 |

## 5. 決済（Stripe）

| 項目                                                                                                                           | 状態               |
| ------------------------------------------------------------------------------------------------------------------------------ | ------------------ |
| 商品と価格（月額 500 円 / 年額 3,980 円）、Webhook（`scripts/stripe-setup.mjs`）                                               | 別スレッドで進行中 |
| Stripe ダッシュボード → 設定 → 公開情報: 事業者名、サポートメール、特商法ページ URL `https://speech-fitness.learn-k.net/legal` | 要作業             |
| Stripe の本番申請（法人情報、代表者の本人確認、銀行口座）                                                                      | 要作業             |
| Checkout の利用規約同意（`consent_collection.terms_of_service`）を有効にするなら、Stripe 側に利用規約 URL を登録               | 任意               |
| 明細書表示名（statement descriptor）を「LEARNK」など分かる名前に                                                               | 要作業             |
| 本番の `STRIPE_SECRET_KEY`, `STRIPE_PRICE_*`, `STRIPE_WEBHOOK_SECRET` を Vercel の環境変数へ                                   | 要作業             |

## 6. ログイン（Google / Supabase）

| 項目                                                                                                                                                                         | 状態   |
| ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------ |
| Google Cloud の OAuth 同意画面を「本番」に公開: アプリ名、ロゴ、ホームページ URL、プライバシーポリシー URL `/privacy`、利用規約 URL `/terms`、承認済みドメイン `learn-k.net` | 要作業 |
| Supabase → Authentication → URL Configuration の Site URL と Redirect URLs に本番ドメインを追加                                                                              | 要作業 |
| `supabase/schema.sql` を本番プロジェクトで実行                                                                                                                               | 要作業 |
| 本番の `EXPO_PUBLIC_SUPABASE_URL` などを Vercel の環境変数へ                                                                                                                 | 要作業 |

## 7. 運用

| 項目                                                                                          | 状態   |
| --------------------------------------------------------------------------------------------- | ------ |
| 問い合わせ窓口 `info@learn-k.net` の受信確認                                                  | 要作業 |
| 退会（アカウント削除）依頼が来たときの手順（Supabase の auth.users 削除と Stripe 顧客の解約） | 要作業 |
| 会計: Stripe の入金と消費税（税込表示済み）、インボイス登録番号を領収書に載せるか             | 要判断 |
| Azure Speech / OpenAI の本番キーと利用上限（予算アラート）                                    | 要作業 |
| README の「現段階では公開しません」を公開後に更新                                             | 要作業 |
