import { LandingScreen } from '@/components/landing/LandingScreen';

/** The landing page at a stable URL, for footer links and sharing. */
export default function WelcomeScreen() {
  return <LandingScreen />;
}
