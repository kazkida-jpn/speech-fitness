import { RecordingPresets, requestRecordingPermissionsAsync, setAudioModeAsync, useAudioPlayer, useAudioRecorder, useAudioRecorderState } from 'expo-audio';
import type { RecordingInput } from 'expo-audio';
import { Link, useLocalSearchParams } from 'expo-router';
import { useMemo, useState } from 'react';
import { Pressable, ScrollView, StyleSheet, Text, View } from 'react-native';
import { SafeAreaView } from 'react-native-safe-area-context';

import { AppHeader } from '@/components/AppHeader';
import { DRILLS, type Drill } from '@/lib/drills';
import { saveDrillHistory } from '@/lib/progress';

const colors={ink:'#19312D',muted:'#60726E',cream:'#F6F3EC',white:'#FFF',green:'#187A64',greenDark:'#0F5E4D',mint:'#DDF4EA',line:'#DCE6E2',coral:'#F06C55'};
const RECORDING_OPTIONS={...RecordingPresets.HIGH_QUALITY,sampleRate:48000,numberOfChannels:1,bitRate:192000,web:{mimeType:'audio/webm;codecs=opus',bitsPerSecond:192000}};

const normalizeInputName=(name:string)=>name.replace(/^(既定|通信|default|communications?)\s*[-–—:]\s*/i,'').trim().toLocaleLowerCase();
const isInputAlias=(input:RecordingInput)=>input.type.toLocaleLowerCase()==='default'||/^(既定|通信|default|communications?)\s*[-–—:]/i.test(input.name);
const deduplicateInputs=(inputs:RecordingInput[])=>Array.from(inputs.reduce((unique,input)=>{const key=normalizeInputName(input.name);const existing=unique.get(key);if(!existing||(isInputAlias(existing)&&!isInputAlias(input)))unique.set(key,input);return unique},new Map<string,RecordingInput>()).values());

export default function DrillsScreen(){
  const params=useLocalSearchParams<{drill?:string}>();
  const initial=useMemo(()=>DRILLS.find((item)=>item.id===params.drill)??null,[params.drill]);
  const [active,setActive]=useState<Drill|null>(initial);
  const [sentenceIndex,setSentenceIndex]=useState(0);
  const [phase,setPhase]=useState<'ready'|'recording'|'recorded'|'complete'>('ready');
  const [recordingUri,setRecordingUri]=useState<string|null>(null);
  const [completedSentences,setCompletedSentences]=useState(0);
  const [completedSeconds,setCompletedSeconds]=useState(0);
  const [lastRecordingSeconds,setLastRecordingSeconds]=useState(0);
  const [message,setMessage]=useState<string|null>(null);
  const [currentInputName,setCurrentInputName]=useState('ブラウザ・端末の既定マイク');
  const [availableInputs,setAvailableInputs]=useState<RecordingInput[]>([]);
  const [selectedInputUid,setSelectedInputUid]=useState<string|null>(null);
  const [isLoadingInputs,setIsLoadingInputs]=useState(false);
  const [isInputListOpen,setIsInputListOpen]=useState(false);
  const recorder=useAudioRecorder(RECORDING_OPTIONS);
  const recorderState=useAudioRecorderState(recorder,100);
  const player=useAudioPlayer(null);

  const chooseDrill=(drill:Drill)=>{player.pause();setActive(drill);setSentenceIndex(0);setPhase('ready');setRecordingUri(null);setCompletedSentences(0);setCompletedSeconds(0);setLastRecordingSeconds(0);setMessage(null)};
  const loadMicrophoneInputs=async()=>{setMessage(null);setIsLoadingInputs(true);try{const permission=await requestRecordingPermissionsAsync();if(!permission.granted){setMessage('マイク一覧を表示するには、マイクの利用許可が必要です。');return}await setAudioModeAsync({allowsRecording:true,playsInSilentMode:true});await recorder.prepareToRecordAsync();const inputs=deduplicateInputs(recorder.getAvailableInputs());const current=await recorder.getCurrentInput();setAvailableInputs(inputs);setCurrentInputName(current.name||current.type||'既定のマイク');setSelectedInputUid((value)=>value??current.uid);setIsInputListOpen(true);recorder.record();await new Promise((resolve)=>setTimeout(resolve,150));if(recorder.getStatus().isRecording)await recorder.stop();await setAudioModeAsync({allowsRecording:false,playsInSilentMode:true})}catch{if(recorder.getStatus().isRecording)await recorder.stop().catch(()=>undefined);setMessage('マイク一覧を取得できませんでした。接続状態を確認してください。')}finally{setIsLoadingInputs(false)}};
  const selectMicrophoneInput=(input:RecordingInput)=>{try{recorder.setInput(input.uid);setSelectedInputUid(input.uid);setCurrentInputName(input.name||input.type||'既定のマイク');setMessage(null)}catch{setMessage('このマイクを選択できませんでした。もう一度一覧を読み込んでください。')}};
  const start=async()=>{setMessage(null);const permission=await requestRecordingPermissionsAsync();if(!permission.granted){setMessage('マイクの利用を許可してください。');return}try{await setAudioModeAsync({allowsRecording:true,playsInSilentMode:true});if(selectedInputUid)recorder.setInput(selectedInputUid);await recorder.prepareToRecordAsync();const current=await recorder.getCurrentInput();setCurrentInputName(current.name||current.type||'既定のマイク');recorder.record();setPhase('recording')}catch{setMessage('録音を開始できませんでした。')}};
  const stop=async()=>{try{const seconds=Math.max(1,Math.round((recorder.getStatus().durationMillis??0)/1000));await recorder.stop();await setAudioModeAsync({allowsRecording:false,playsInSilentMode:true});setRecordingUri(recorder.uri);setLastRecordingSeconds(seconds);setCompletedSeconds((value)=>value+seconds);setPhase('recorded')}catch{setMessage('録音を終了できませんでした。')}};
  const play=async()=>{if(!recordingUri)return;setMessage(null);try{await setAudioModeAsync({allowsRecording:false,playsInSilentMode:true});player.pause();player.replace(recordingUri);player.muted=false;player.volume=1;player.playbackRate=1;await player.seekTo(0);player.play()}catch{setMessage('録音を再生できませんでした。もう一度録音してください。')}};
  const completeSentence=async()=>{if(!active)return;const nextCount=completedSentences+1;setCompletedSentences(nextCount);await saveDrillHistory({drillId:active.id,durationSeconds:lastRecordingSeconds,sentenceCount:1});if(sentenceIndex>=active.sentences.length-1){setPhase('complete')}else{setSentenceIndex((value)=>value+1);setRecordingUri(null);setLastRecordingSeconds(0);setPhase('ready')}};

  return <SafeAreaView style={styles.safeArea}><ScrollView contentContainerStyle={styles.container}>
    <AppHeader />
    {!active? <><Text style={styles.hero}>今日鍛えるところを選びましょう</Text><Text style={styles.intro}>点数はつきません。例文を読み、自分の声を聞いて、少しずつ習慣にします。</Text><View style={styles.grid}>{DRILLS.map((drill)=><Pressable key={drill.id} style={[styles.drillCard,{backgroundColor:drill.accent}]} onPress={()=>chooseDrill(drill)}><Text style={styles.drillTitle}>{drill.title}</Text><Text style={styles.drillBody}>{drill.description}</Text><Text style={styles.startLink}>始める →</Text></Pressable>)}</View></>:
    <View>
      <Pressable onPress={()=>setActive(null)}><Text style={styles.change}>‹ ドリル一覧へ</Text></Pressable>
      <Text style={styles.hero}>{active.title}</Text><Text style={styles.intro}>{active.description}</Text>
      {phase==='complete'?<View style={styles.completeCard}><Text style={styles.completeMark}>✓</Text><Text style={styles.completeTitle}>今日のドリル完了</Text><Text style={styles.completeValue}>{completedSentences}文・約{Math.max(1,Math.round(completedSeconds/60))}分</Text><Text style={styles.completeNote}>今日の発話カレンダーに記録しました。</Text><Link href="/" asChild><Pressable style={styles.primary}><Text style={styles.primaryText}>ホームへ戻る</Text></Pressable></Link></View>:
      <View style={styles.practiceCard}>
        <View style={styles.progressRow}><Text style={styles.progress}>例文 {sentenceIndex+1} / {active.sentences.length}</Text><Text style={styles.progress}>{completedSentences}文完了</Text></View>
        <Text style={styles.sentence}>{active.sentences[sentenceIndex]}</Text>
        <View style={styles.inputCard}><View style={styles.inputIndicator}/><View style={styles.inputTextWrap}><Text style={styles.inputLabel}>使用するマイク</Text><Text style={styles.inputName}>{currentInputName}</Text><Text style={styles.inputMeta}>{availableInputs.length>0?`${availableInputs.length}台の入力デバイスを認識`:'選択しない場合は端末の既定マイクを使います'}</Text></View>{phase==='ready'&&<Pressable style={[styles.inputAction,isLoadingInputs&&styles.inputActionDisabled]} onPress={availableInputs.length>0?()=>setIsInputListOpen(true):loadMicrophoneInputs} disabled={isLoadingInputs}><Text style={styles.inputActionText}>{isLoadingInputs?'確認中…':availableInputs.length>0?'変更':'選ぶ'}</Text></Pressable>}</View>
        {phase==='ready'&&isInputListOpen&&availableInputs.length>0&&<View style={styles.inputList}><Text style={styles.inputListTitle}>使用するマイクを選択</Text>{availableInputs.map((input)=>{const selected=selectedInputUid===input.uid;return <Pressable key={input.uid} style={[styles.inputOption,selected&&styles.inputOptionSelected]} onPress={()=>selectMicrophoneInput(input)}><View style={[styles.radio,selected&&styles.radioSelected]}>{selected&&<View style={styles.radioDot}/>}</View><View style={styles.inputOptionTextWrap}><Text style={styles.inputOptionName}>{input.name}</Text><Text style={styles.inputOptionType}>{input.type}</Text></View></Pressable>})}<Pressable style={styles.inputConfirmButton} onPress={()=>setIsInputListOpen(false)}><Text style={styles.inputConfirmButtonText}>このマイクに決定</Text></Pressable></View>}
        <View style={styles.micWrap}><View style={[styles.mic,phase==='recording'&&styles.micActive]}><Text style={styles.micDot}>●</Text></View><Text style={styles.timer}>{Math.floor((recorderState.durationMillis??0)/1000)}秒</Text><Text style={styles.status}>{phase==='recording'?'録音中です':phase==='recorded'?'録音できました。自分の声を聞いてみましょう。':'準備ができたら録音してください。'}</Text></View>
        {phase==='ready'&&<Pressable style={styles.primary} onPress={start}><Text style={styles.primaryText}>録音を始める</Text></Pressable>}
        {phase==='recording'&&<Pressable style={styles.stop} onPress={stop}><Text style={styles.stopText}>録音を終了する</Text></Pressable>}
        {phase==='recorded'&&<View style={styles.actions}><Pressable style={styles.secondary} onPress={play}><Text style={styles.secondaryText}>▶ 自分の声を聞く</Text></Pressable><Pressable style={styles.primary} onPress={completeSentence}><Text style={styles.primaryText}>{sentenceIndex===active.sentences.length-1?'ドリルを完了する':'完了して次の例文へ'}</Text></Pressable></View>}
        {message&&<Text style={styles.error}>{message}</Text>}
      </View>}
    </View>}
  </ScrollView></SafeAreaView>;
}

const styles=StyleSheet.create({safeArea:{flex:1,backgroundColor:colors.cream},container:{width:'100%',maxWidth:820,alignSelf:'center',padding:22,paddingBottom:50},topbar:{flexDirection:'row',alignItems:'center',justifyContent:'space-between',marginBottom:25},back:{color:colors.green,fontSize:13,fontWeight:'700'},logo:{color:colors.ink,fontSize:19,fontWeight:'800'},hero:{color:colors.ink,fontSize:27,lineHeight:35,fontWeight:'800'},intro:{color:colors.muted,fontSize:14,lineHeight:22,marginTop:7,marginBottom:20},grid:{flexDirection:'row',flexWrap:'wrap',gap:10},drillCard:{flexGrow:1,flexBasis:300,borderRadius:18,padding:18,minHeight:155},drillTitle:{color:colors.ink,fontSize:17,fontWeight:'800'},drillBody:{color:colors.muted,fontSize:12,lineHeight:18,marginTop:7},startLink:{color:colors.greenDark,fontSize:12,fontWeight:'800',marginTop:'auto',paddingTop:12},change:{color:colors.green,fontSize:12,fontWeight:'700',marginBottom:13},practiceCard:{backgroundColor:colors.white,borderRadius:24,padding:22,borderWidth:1,borderColor:colors.line},progressRow:{flexDirection:'row',justifyContent:'space-between'},progress:{color:colors.green,fontSize:11,fontWeight:'800'},sentence:{color:colors.ink,fontSize:23,lineHeight:38,fontWeight:'600',backgroundColor:colors.mint,borderRadius:18,padding:20,marginTop:15},inputCard:{flexDirection:'row',alignItems:'center',gap:10,backgroundColor:colors.cream,borderRadius:15,padding:13,marginTop:14},inputIndicator:{width:9,height:9,borderRadius:5,backgroundColor:colors.green},inputTextWrap:{flex:1},inputLabel:{color:colors.muted,fontSize:10,fontWeight:'700'},inputName:{color:colors.ink,fontSize:13,fontWeight:'800',marginTop:2},inputMeta:{color:colors.muted,fontSize:10,marginTop:2},inputAction:{backgroundColor:colors.white,borderWidth:1,borderColor:colors.line,borderRadius:12,paddingHorizontal:14,paddingVertical:9},inputActionDisabled:{opacity:.55},inputActionText:{color:colors.greenDark,fontSize:12,fontWeight:'800'},inputList:{backgroundColor:colors.white,borderWidth:1,borderColor:colors.line,borderRadius:15,padding:9,marginTop:8,gap:5},inputListTitle:{color:colors.muted,fontSize:11,fontWeight:'700',padding:5},inputOption:{flexDirection:'row',alignItems:'center',gap:10,borderRadius:11,padding:10},inputOptionSelected:{backgroundColor:colors.mint},radio:{width:18,height:18,borderRadius:9,borderWidth:2,borderColor:colors.line,alignItems:'center',justifyContent:'center'},radioSelected:{borderColor:colors.green},radioDot:{width:8,height:8,borderRadius:4,backgroundColor:colors.green},inputOptionTextWrap:{flex:1},inputOptionName:{color:colors.ink,fontSize:13,fontWeight:'700'},inputOptionType:{color:colors.muted,fontSize:10,marginTop:2},inputConfirmButton:{backgroundColor:colors.green,borderRadius:12,alignItems:'center',paddingVertical:11,marginTop:4},inputConfirmButtonText:{color:colors.white,fontSize:13,fontWeight:'800'},micWrap:{alignItems:'center',paddingVertical:23},mic:{width:65,height:65,borderRadius:33,backgroundColor:colors.green,alignItems:'center',justifyContent:'center'},micActive:{backgroundColor:colors.coral},micDot:{color:colors.white,fontSize:18},timer:{color:colors.ink,fontSize:25,fontWeight:'800',marginTop:8},status:{color:colors.muted,fontSize:12,marginTop:4,textAlign:'center'},primary:{backgroundColor:colors.green,borderRadius:15,alignItems:'center',paddingVertical:14},primaryText:{color:colors.white,fontSize:14,fontWeight:'800'},stop:{backgroundColor:'#FFF0ED',borderRadius:15,alignItems:'center',paddingVertical:14},stopText:{color:'#B63D2C',fontSize:14,fontWeight:'800'},actions:{gap:10},secondary:{borderWidth:1,borderColor:colors.green,borderRadius:15,alignItems:'center',paddingVertical:13},secondaryText:{color:colors.greenDark,fontSize:14,fontWeight:'800'},error:{color:'#B63D2C',fontSize:12,textAlign:'center',marginTop:10},completeCard:{backgroundColor:colors.white,borderRadius:24,padding:28,alignItems:'center',borderWidth:1,borderColor:colors.line},completeMark:{color:colors.green,fontSize:38,fontWeight:'800'},completeTitle:{color:colors.ink,fontSize:23,fontWeight:'800',marginTop:6},completeValue:{color:colors.greenDark,fontSize:18,fontWeight:'800',marginTop:10},completeNote:{color:colors.muted,fontSize:12,marginTop:5,marginBottom:20}});
